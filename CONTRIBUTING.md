# Руководство по внесению вклада в ST7789V3 Library

Спасибо за интерес к проекту ST7789V3 Library! Мы приветствуем вклад от сообщества и хотим сделать процесс внесения изменений максимально простым и прозрачным.

## 📋 Содержание

- [Кодекс поведения](#кодекс-поведения)
- [Как внести вклад](#как-внести-вклад)
- [Сообщение об ошибках](#сообщение-об-ошибках)
- [Предложение улучшений](#предложение-улучшений)
- [Разработка](#разработка)
- [Стиль кодирования](#стиль-кодирования)
- [Тестирование](#тестирование)
- [Документация](#документация)

## 📜 Кодекс поведения

Этот проект и все участники должны соблюдать [Кодекс поведения](CODE_OF_CONDUCT.md). Участвуя в проекте, вы соглашаетесь соблюдать эти правила.

## 🤝 Как внести вклад

### Быстрый старт

1. **Fork** репозитория
2. **Клонируйте** свой fork
3. **Создайте** branch для новой функции
4. **Внесите** изменения
5. **Протестируйте** изменения
6. **Отправьте** Pull Request

### Подробные шаги

```bash
# 1. Fork репозитория через GitHub веб-интерфейс

# 2. Клонирование
git clone https://github.com/yourusername/st7789v3.git
cd st7789v3

# 3. Добавление upstream
git remote add upstream https://github.com/originalowner/st7789v3.git

# 4. Создание branch
git checkout -b feature/your-feature-name

# 5. Внесение изменений
# ... редактируйте файлы ...

# 6. Commit изменений
git add .
git commit -m "feat: add your feature description"

# 7. Push в ваш fork
git push origin feature/your-feature-name

# 8. Создание Pull Request через GitHub
```

## 🐛 Сообщение об ошибках

Перед созданием issue:

### Проверьте существующие issue
- Поищите похожие проблемы в [открытых](https://github.com/yourusername/st7789v3/issues) и [закрытых](https://github.com/yourusername/st7789v3/issues?q=is%3Aissue+is%3Aclosed) issue

### Шаблон сообщения об ошибке

```markdown
**Описание ошибки**
Краткое и ясное описание проблемы.

**Воспроизведение**
Шаги для воспроизведения:
1. Перейдите к '...'
2. Нажмите на '...'
3. Прокрутите вниз до '...'
4. Увидите ошибку

**Ожидаемое поведение**
Описание того, что должно происходить.

**Скриншоты**
Если применимо, добавьте скриншоты.

**Окружение:**
- Контроллер: STM32F411CEU6
- Дисплей: ST7789V3 240x320
- Версия библиотеки: 1.0.0
- IDE: VSCode/STM32CubeIDE
- Частота SPI: 16 МГц

**Дополнительный контекст**
Любая другая информация о проблеме.

**Код для воспроизведения**
```cpp
// Минимальный код для воспроизведения ошибки
```
```

## 💡 Предложение улучшений

### Шаблон предложения функции

```markdown
**Описание функции**
Краткое описание предлагаемой функции.

**Проблема**
Описание проблемы, которую решает эта функция.

**Предлагаемое решение**
Подробное описание предлагаемого решения.

**Альтернативы**
Альтернативные решения, которые вы рассматривали.

**Примеры использования**
```cpp
// Примеры кода использования новой функции
display.newFunction(parameters);
```

**Дополнительная информация**
Любая другая информация или скриншоты.
```

## 🔧 Разработка

### Настройка среды разработки

1. **Требования:**
   - STM32F411CEU6 или совместимый
   - ST7789V3 дисплей 240x320
   - VSCode с расширениями STM32
   - STM32CubeMX
   - HAL библиотека

2. **Настройка:**
   ```bash
   # Клонирование
   git clone https://github.com/yourusername/st7789v3.git
   cd st7789v3
   
   # Настройка окружения
   # Настройте пути к HAL библиотеке в вашей IDE
   ```

### Структура проекта

```
st7789v3/
├── fonts/              # Шрифты
│   ├── font8x16.cpp
│   └── font8x16.hpp
├── framebuffer/        # Буфер кадра
│   ├── framebuffer.cpp
│   └── framebuffer.hpp
├── inc/                # Заголовочные файлы
│   ├── st7789v3.hpp
│   └── st7789v3_config.hpp
├── src/                # Исходный код
│   └── st7789v3.cpp
├── examples/           # Примеры (создайте если нужно)
├── tests/             # Тесты (создайте если нужно)
└── docs/              # Дополнительная документация
```

## 📝 Стиль кодирования

### C++ Стандарт
- Используйте **C++17**
- Следуйте [Google C++ Style Guide](https://google.github.io/styleguide/cppguide.html)

### Именование

```cpp
// Классы: PascalCase
class DisplayController {};

// Функции и переменные: camelCase
void initDisplay();
uint16_t pixelColor;

// Константы: UPPER_SNAKE_CASE
constexpr uint16_t MAX_WIDTH = 240;

// Приватные члены класса: trailing underscore
class MyClass {
private:
    uint16_t width_;
    uint16_t height_;
};

// Namespace: PascalCase
namespace ST7789_Colors {}
```

### Форматирование

```cpp
// Отступы: 4 пробела (не табы)
if (condition) {
    doSomething();
}

// Фигурные скобки на новой строке для функций
void functionName() 
{
    // код
}

// Фигурные скобки на той же строке для if/for/while
if (condition) {
    // код
}

// Длинные строки: перенос на 100 символов
void longFunctionName(uint16_t parameter1, 
                     uint16_t parameter2,
                     uint16_t parameter3);
```

### Комментарии

```cpp
/**
 * @brief Краткое описание функции
 * @param x Координата X
 * @param y Координата Y  
 * @param color Цвет в формате RGB565
 * @return true если успешно, false при ошибке
 */
bool drawPixel(uint16_t x, uint16_t y, uint16_t color);

// Однострочный комментарий для пояснения
uint16_t color = rgb565(255, 0, 0); // Красный цвет
```

### Обработка ошибок

```cpp
// Проверка входных параметров
if (x >= width_ || y >= height_) {
    return false; // или обработка ошибки
}

// Проверка указателей
if (buffer_ == nullptr) {
    return false;
}
```

## 🧪 Тестирование

### Требования к тестированию

1. **Тестируйте на реальном оборудовании**
2. **Проверьте все новые функции**
3. **Убедитесь, что существующие тесты проходят**

### Примеры тестов

```cpp
// Тест базовой функциональности
void test_basic_drawing() {
    ST7789V3 display(&hspi1, cs_pin, dc_pin, rst_pin);
    display.init();
    
    // Тест заливки экрана
    display.fillScreen(ST7789_Colors::RED);
    HAL_Delay(1000);
    
    // Тест рисования пикселя
    display.drawPixel(120, 160, ST7789_Colors::WHITE);
    
    // Визуальная проверка результата
}

// Тест производительности
void test_performance() {
    uint32_t start_time = HAL_GetTick();
    
    // Выполнение операции
    display.fillScreen(ST7789_Colors::BLUE);
    
    uint32_t elapsed = HAL_GetTick() - start_time;
    // Проверка, что время в пределах ожидаемого
}
```

### Контрольный список тестирования

- [ ] Компиляция без предупреждений
- [ ] Все существующие функции работают
- [ ] Новые функции протестированы
- [ ] Проверка граничных случаев
- [ ] Тест производительности
- [ ] Проверка использования памяти
- [ ] Тест на разных настройках SPI
- [ ] Визуальная проверка на дисплее

## 📚 Документация

### Обновление документации

При добавлении новых функций:

1. **Обновите README.md**
2. **Добавьте комментарии в код**
3. **Создайте примеры использования**
4. **Обновите CHANGELOG.md**

### Примеры в коде

```cpp
/**
 * @example
 * ```cpp
 * ST7789V3 display(&hspi1, cs_pin, dc_pin, rst_pin);
 * display.init();
 * display.drawCircle(120, 160, 50, ST7789_Colors::RED);
 * ```
 */
void drawCircle(uint16_t x0, uint16_t y0, uint16_t r, uint16_t color);
```

## 🚀 Процесс Pull Request

### Контрольный список PR

- [ ] Код соответствует стилю проекта
- [ ] Все тесты проходят
- [ ] Документация обновлена
- [ ] CHANGELOG.md обновлен
- [ ] Commit сообщения следуют соглашению
- [ ] PR описание заполнено

### Соглашение о commit сообщениях

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
type(scope): description

feat: add new drawing function
fix: resolve memory leak in framebuffer  
docs: update API documentation
test: add performance tests
refactor: optimize pixel drawing
```

Типы:
- `feat`: новая функция
- `fix`: исправление ошибки
- `docs`: изменения в документации
- `test`: добавление или изменение тестов
- `refactor`: рефакторинг кода
- `perf`: улучшение производительности
- `chore`: обновление зависимостей, конфигурации

### Шаблон описания PR

```markdown
## Описание
Краткое описание изменений.

## Тип изменения
- [ ] Исправление ошибки (fix)
- [ ] Новая функция (feat)
- [ ] Критическое изменение (breaking change)
- [ ] Обновление документации (docs)

## Тестирование
- [ ] Протестировано на STM32F411CEU6
- [ ] Все существующие тесты проходят
- [ ] Добавлены новые тесты (если применимо)

## Скриншоты
Если применимо, добавьте скриншоты результатов.

## Дополнительная информация
Любая дополнительная информация о PR.
```

## ❓ Вопросы

Если у вас есть вопросы:

- 📧 Email: support@yourproject.com
- 💬 GitHub Discussions
- 📖 Проверьте документацию в README.md

## 🎉 Признательность

Спасибо всем, кто вносит вклад в проект! Ваши усилия делают эту библиотеку лучше для всего сообщества STM32.

---

**Помните: любой вклад, большой или маленький, ценен! 🚀**
